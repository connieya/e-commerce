spring:
  datasource:
    url: jdbc:h2:mem:dev;MODE=MYSQL
    username: sa
  profiles:
    active: test
  thymeleaf:
    prefix: classpath:/templates/
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
      enabled: true
